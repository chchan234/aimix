# API Configuration
# No VITE_API_URL needed - API uses relative paths in production
# Development: Proxied through Vite dev server to localhost:3000
# Production: Same domain as client (monorepo deployment)
