# ====================================
# Server Environment Variables
# ====================================

# Database - Supabase PostgreSQL (Direct Connection)
# Get from: Supabase Project → Settings → Database → Connection String
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Supabase Configuration
# CRITICAL: Use SERVICE_ROLE_KEY on server (bypasses RLS, admin access)
# Get from: Supabase Project → Settings → API → service_role key
SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# AI API Keys
OPENAI_API_KEY=sk-proj-...
GEMINI_API_KEY=AIzaSy...
# Note: GOOGLE_AI_API_KEY is same as GEMINI_API_KEY (for backward compatibility)

# Authentication
# CRITICAL: Must be at least 32 characters for production
JWT_SECRET=your-secret-key-change-this-min-32-chars

# OAuth - Kakao
KAKAO_REST_API_KEY=your-kakao-rest-api-key

# Email - SendGrid
SENDGRID_API_KEY=SG.your-sendgrid-api-key
EMAIL_FROM=noreply@your-domain.com

# Client URL (for OAuth callbacks and CORS)
CLIENT_URL=http://localhost:5173

# Server Configuration
PORT=3000
NODE_ENV=development

# Redis (Optional - for caching and rate limiting)
# For production: Use managed Redis (Upstash, Redis Cloud, etc.)
REDIS_URL=redis://localhost:6379
